# Local URL for the backend Campaign Copilot API (Node.js/Express). Do not put OpenAI credentials here.
VITE_API_BASE_URL=http://localhost:3001

# Local URL for the Python/FastAPI backend (auth, bookings, influencer profiles).
VITE_BACKEND_URL=http://localhost:8000
